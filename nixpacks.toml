[phases.setup]
nixPkgs = ['nodejs_22']

[phases.install]
cmds = [
  'npm install -g corepack@latest',
  'corepack enable',
  'corepack prepare pnpm@10.29.3 --activate',
  'pnpm i --frozen-lockfile'
]
